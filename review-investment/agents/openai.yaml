interface:
  display_name: "Investment Rule Review"
  short_description: "Use Eastmoney data to check an investment against your personal rules"
  default_prompt: "Use $review-investment to review an investment and decide whether it meets my rules."

dependencies:
  tools:
    - type: "mcp"
      value: "mx-ds-mcp"
      description: "Eastmoney financial data, announcements, and macro data"

policy:
  allow_implicit_invocation: true
